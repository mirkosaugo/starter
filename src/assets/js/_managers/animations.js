import _                from "underscore"
import ScrollMagic      from "scrollmagic"
import TweenLite        from "gsap"
import inView           from 'in-view'
import {x, isTouch}     from '../_helpers'

const Animations = (function() {

    const _init = () => {
        // init animations
    }

    return {
        init: _init
    }

}())

export default Animations

